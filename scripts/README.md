# Scripts

Utility scripts for build, deployment, evaluation, and demo automation.

Current scripts:

- `export-demo-state.py`: prints simulated robot platform state or submits a mock cloud training job.
